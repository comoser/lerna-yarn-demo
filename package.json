{
  "name": "main-app",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start": "yarn workspaces run start",
    "release": "export $(xargs < .env) && npx lerna version patch --conventional-commits --create-release github --yes"
  }
}
