{
  "name": "main-app",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start": "yarn workspaces run start",
    "release:patch": "export $(xargs < .env) && npx lerna version patch --conventional-commits --create-release github --yes",
    "release:minor": "export $(xargs < .env) && npx lerna version minor --conventional-commits --create-release github --yes",
    "release:major": "export $(xargs < .env) && npx lerna version major --conventional-commits --create-release github --yes"
  }
}
